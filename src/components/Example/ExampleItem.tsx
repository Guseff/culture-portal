import React from 'react';

export const ExampleItem = ({ author }: any) => {
  console.log('in ExampleItem', author);

  return (
    <>
      {/* <div>{`Name: ${author.name}`}</div>
      <div>{`Description ${author.description}`}</div> */}
      <div>noop</div>
    </>
  );
};
